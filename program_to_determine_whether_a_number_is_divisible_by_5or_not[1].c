#include<stdio.h>
int main()
{
 int num;
printf("the num:");  
scanf("%d",&num);
//it is true if the number is completely divisible by 5
if(num%=0)
printf("%d is divisible.",num);
else
printf("%d is not divisible.",num);
printf("to determine whether a number is divible by 5or not");
return 0;
}